import { AddCandidateForm } from "@/components/add-candidate-form";

export const metadata = { title: "Add candidate · Design Challenge Tracker" };

export default function Page() {
  return <AddCandidateForm />;
}
